<!DOCTYPE html>
<html lang=”en”>
<head>
    <meta charset=”UTF-8”>
    <meta name=”viewport” content=”width=device-width, initial-scale=1.0”>
    <title>Basic Website</title>
    <style>
        /* Define your color scheme */
        Body {
            Background-color: #f0f0f0;
            Font-family: Arial, sans-serif;
            Margin: 0;
            Padding: 0;
        }

        /* Border Styles */
        .border-example {
            Border: 2px solid #333;
            Padding: 10px;
            Margin: 10px;
        }

        /* Text Styles */
        .text-example {
            Font-size: 18px;
            Color: #555;
            Text-align: center;
        }

        /* Images */
        .image-example {
            Max-width: 100%;
            Height: auto;
        }

        /* Input Tags */
        .input-example {
            Width: 100%;
            Padding: 10px;
            Margin: 10px 0;
            Border: 1px solid #ccc;
            Border-radius: 5px;
        }

        /* HR Tag */
        .hr-example {
            Border: none;
            Border-top: 1px solid #ddd;
            Margin: 20px 0;
        }

        /* Buttons */
        .button-example {
            Background-color: #007bff;
            Color: #fff;
            Padding: 10px 20px;
            Border: none;
            Border-radius: 5px;
            Cursor: pointer;
        }

        /* Margin and Padding */
        .margin-example {
            Margin: 20px;
        }

        .padding-example {
            Padding: 20px;
        }
    </style>
</head>
<body>
    <h1 class=”text-example”>Basic Website</h1>

    <div class=”border-example”>
        <p class=”text-example”>This is a bordered box with some text inside.</p>
    </div>

    <img class=”image-example” src=”your-image.jpg” alt=”Sample Image”>

    <input class=”input-example” type=”text” placeholder=”Input Field”>

    <hr class=”hr-example”>

    <button class=”button-example”>Submit</button>

    <div class=”margin-example”>
        <p class=”text-example”>This content has margin applied.</p>
    </div>

    <div class=”padding-example”>
        <p class=”text-example”>This content has padding applied.</p>
    </div>
</body>
</html>

